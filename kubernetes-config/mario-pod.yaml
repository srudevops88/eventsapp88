apiVersion: v1
kind: pod
metadata:
  name: supermario-pod
  labels:
    app: mario
spec:
  containers:
  - name: supermario-demo
    image: pengbai/docker-supermario
    ports:
    - containerPort: 8080